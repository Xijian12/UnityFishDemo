# FishingDemo 重构升级优化日志（全量汇总）

> 记录范围：从项目重构启动到当前版本。  
> 目标：完整沉淀“做过什么升级、为什么做、收益是什么”，便于后续联调、性能优化和服务端接入。

---

## 1. 项目方向与技术路线升级

### 升级点
- 从 Unity 2D 模板工程升级为 3D 捕鱼玩法工程（不重建模板，直接在现有工程演进）。
- 相机方案从 FPS 讨论路线收敛到“45-60 度俯视 + 可微调 + 慢镜头能力”。
- 明确工程目标：模块解耦、对象池友好、配置驱动、后续可接服务端同步。

### 收益
- 保留已有资产与功能，迁移成本低。
- 玩法可视性更好（全局俯瞰鱼群区域）。
- 为后续网络化与工程化扩展打下基础。

---

## 2. 目录结构与分层治理

### 升级点
- 统一脚本目录迁移到 `Assets/Scripts/Runtime`：
  - `Runtime/Gameplay`
  - `Runtime/Presentation`
  - `Runtime/Infrastructure`
  - `Runtime/Shared`
- 迁移时同步移动 `.meta`，确保 GUID 稳定，避免场景/Prefab 丢引用。

### 收益
- 目录语义清晰，职责边界明确。
- 降低长期维护成本，便于多人协作与新同学上手。

---

## 3. 运行时更新模型升级（集中调度）

### 升级点
- `Bullet` 从自 `Update()` 改为 `BulletManager` 统一 `ManualUpdate`。
- `Fish` 保持由 `FishManager` 统一调度更新。
- 引入“外部移动控制”开关，支持鱼群系统接管鱼移动，避免双重驱动。

### 收益
- 更新入口统一，更易做性能治理和分帧策略。
- 降低行为冲突（组件各自 Update 抢控制权）。

---

## 4. 鱼系统解耦重构（Fish / Combat / Visual / Movement）

### 升级点
- `Fish` 变为编排层（orchestrator），负责流程与生命周期。
- `FishCombat` 负责血量/死亡状态机（`IsDead/IsDying/TryEnterDeath`）。
- `FishVisual` 负责受击、死亡表现与复用重置。
- `FishMovement` 负责曲线移动与朝向。
- `FishKilledEvent` 发布点统一在 `Fish`，避免跨模块事件职责混乱。

### 收益
- 单一职责清晰，问题定位成本显著降低。
- 为后续技能系统、网络同步、表现扩展提供稳定边界。

---

## 5. 路径运动升级：三阶贝塞尔 + 3D 规则

### 升级点
- 新增 `BezierUtility`（点、切线、长度近似）。
- 鱼移动统一为三阶贝塞尔，支持自动朝向切线方向。
- 活动空间约束到 XZ 主平面（Y 用轻微浮动表现）。
- 运动与帧率解耦（`deltaTime`）。

### 收益
- 路径更自然，运动质量提升。
- 代码可复用到鱼群主路径、特殊鱼轨迹与镜头演出。

---

## 6. 相机系统升级（TopDownCameraController）

### 升级点
- 固定位置与固定俯角，不再每帧重算跟随坐标。
- 支持小范围 Yaw 微调。
- 支持慢镜头更新时间策略（可用 unscaled time）。
- 相机抖动能力统一归口到相机脚本，不再散落业务脚本。

### 收益
- 视角稳定，操作感更一致。
- 相机能力可复用，可控性更高。

---

## 7. 发射链路架构收敛（CannonController / BulletSpawner）

### 升级点
- 明确职责：
  - `CannonController`：输入、模式、方向、触发时机
  - `BulletSpawner`：对象池取子弹 + 初始化发射
- 支持手动瞄准与自动索敌（最近鱼）。
- 鱼与子弹的 3D/XZ 逻辑统一。

### 收益
- 消除“多入口发射”的冲突与重复逻辑。
- 便于以后接入服务端发射权威校验。

---

## 8. 鱼群系统从 0 到 1（配置驱动 + 运行时实例）

### 升级点
- 新建 `FishGroup` 作为纯 C# 实例（非 `MonoBehaviour`）。
- 新建 `FishGroupManager` 负责生成计划、池准备、生命周期驱动。
- 新建/完善 `FishGroupConfig`（阵型、方向、速度、波次、对象池参数）。
- 新建 `FormationCalculator` 阵型算法，支持直线/圆形/V 字。
- 新建 `FishSpawnMode` + `FishSpawnModeController`，可切换“单鱼刷怪/鱼群刷怪”，避免双系统同时生效。

### 收益
- 鱼群逻辑可测试、可扩展、可配置。
- 模式切换清晰，联调成本低。

---

## 9. 鱼群系统二次重构（成员抽象 + 三阶段生命周期）

### 升级点
- 新增 `FishGroupMember`，成员数据结构抽象化，字段包含：
  - 鱼实例
  - 槽位 index
  - 局部偏移
  - 存活状态
  - 脱队状态
  - FishMovement 引用
  - Transform 缓存
- `FishGroup` 统一持有“主路径 + 成员偏移 + 回收边界”。
- 鱼群移动生命周期升级为三阶段：
  1. 路径移动阶段：按贝塞尔推进  
  2. 离场阶段：`t >= 1` 后沿末端切线继续游出  
  3. 越界回收阶段：超出战斗区外扩边界后再回收
- 鱼群内速度统一使用 `groupSpeed`，移除成员起跑延迟方案。

### 收益
- 群体行为更自然，离场更符合预期。
- 数据结构清晰，便于后续做编队行为/断队行为。

---

## 10. 关键问题修复记录（功能正确性）

### 10.1 协程启动报错（inactive object）
- 现象：`Coroutine couldn't be started because game object is inactive`
- 修复：回收前后协程统一停止；受击/死亡流程增加状态防御。

### 10.2 对象池复用后鱼变白
- 根因：视觉状态恢复不完整，默认色缓存与回收时机问题。
- 修复：缓存 prefab 默认色；`OnSpawn/OnRecycle` 强制恢复视觉状态。

### 10.3 鱼群模式无鱼生成
- 根因：`FishGroupManager.groupConfigs` 为空；场景未正确挂载/绑定配置。
- 修复：补齐配置资产、模式控制器、生成计划构建与候选消耗。

### 10.4 阵型方向错误
- 问题：直线/V 字朝向与移动方向不一致。
- 修复：统一局部前进轴约定；修正线阵与 V 字阵坐标定义。

### 10.5 鱼群卡顿/闪烁
- 根因：`FishManager` 隔帧更新策略导致视觉抖动。
- 修复：恢复每帧更新，去掉隔帧补偿。

### 10.6 `Look rotation viewing vector is zero`
- 根因：鱼群离场方向 `_exitDirection` 未初始化即设置 `transform.forward`。
- 修复：`Spawn` 计算末端切线并兜底；`Tick` 前向赋值前加零向量保护。

---

## 11. UI 金币与得分反馈系统升级

### 升级点
- 明确当前阶段全部走 2D UI 坐标系金币链路（后续再扩展 3D 特殊金币）。
- `UIFxManager` 统一管理金币生成与浮字生成。
- `CoinUIFx` 负责金币运动表现；`FloatingScoreTextFx` 负责浮字表现。
- 事件链路统一：
  - `FishKilledEvent` -> 触发金币飞行
  - `CoinArrivedEvent` -> 触发浮动加分文字

### 收益
- 职责清晰，UI 特效链路稳定。
- 后续扩展特殊金币不会污染主链路。

---

## 12. 动画系统升级：协程/Lerp -> DOTween

### 升级点
- 已切换到 DOTween 的模块：
  - `FishVisual`：受击闪白、死亡缩放淡出
  - `Fish`：受击/死亡动画驱动改为 tween 生命周期
  - `CoinUIFx`：金币飞行动画
  - `FloatingScoreTextFx`：浮字上浮+淡出
  - `ScoreManager`：COMBO/NICE 淡出
  - `TopDownCameraController`：相机抖动衰减
- 对象池复用场景统一做 `Kill()`，避免 tween 残留和状态穿透。

### 收益
- 动画代码更简洁、可读性更高。
- 动画曲线与时序更可控，表现一致性提升。

---

## 13. 事件系统与模块耦合治理

### 升级点
- 修复 `EventBusClass` 可见性相关问题（通过职责迁移而非“到处引 using”）。
- 视觉组件不直接依赖总线实现，改由管理器统一发布/中转事件。

### 收益
- 降低模块耦合，减少命名空间/程序集边界问题。

---

## 14. 配置驱动能力增强

### 升级点
- `FishGroupConfig` 支持阵型、方向、速度、鱼数、波次、对象池容量。
- 鱼群总量从“代码固定”转为“配置可控”。

### 收益
- 调参效率提升，可快速做关卡化或活动化配置。

---

## 15. 当前架构状态（阶段总结）

- 运动系统：单鱼/鱼群均已工程化，可配置、可扩展。
- 动画系统：主要表现动画已迁移到 DOTween。
- 池化系统：鱼、子弹、UI 特效均为对象池路径。
- 事件系统：核心事件链路稳定，职责边界较清晰。
- 相机系统：固定俯视 + 微调 + 抖动能力已独立归口。

---

## 16. 后续优化建议（下一阶段）

1. 为 `FishGroup` 增加可视化 Gizmos（主路径、回收边界、离场切线）。
2. 为 DOTween 统一规范：
   - `SetUpdate` 策略
   - `Ease` 约定
   - `Kill` 时机规范（OnRecycle/OnDisable/OnDestroy）
3. 补充鱼群行为扩展：
   - 脱队行为（受击/技能驱散）
   - 编队重组
4. 增加性能观测点：
   - 活鱼数、活跃 tween 数、池命中率、GC Alloc 监控。
5. 增加回放/同步友好字段，为服务端权威同步预留数据通道。

